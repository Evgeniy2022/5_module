let number = +prompt(meaning);
if (typeof number === 'number'){
		if (i % 2 === 0 && i % 2 != isNaN(isNaN(i))){
			console.log(`Число ${number} четное`);
		} else{
			console.log(`Число ${number} не четное`);
		}
} else{
	console.log('Упс, кажется, вы ошиблись');
}

